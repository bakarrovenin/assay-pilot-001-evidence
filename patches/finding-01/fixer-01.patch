diff --git a/app/corpus_app.py b/app/corpus_app.py
index 7d9180c..8b02c12 100644
--- a/app/corpus_app.py
+++ b/app/corpus_app.py
@@ -31,8 +31,8 @@ def product_lookup():
     conn = get_connection()
     cur = conn.cursor()
 
-    query = f"SELECT id, name, price, category, sku FROM products WHERE name = '{name}'"
-    rows = cur.execute(query).fetchall()
+    query = "SELECT id, name, price, category, sku FROM products WHERE name = ?"
+    rows = cur.execute(query, (name,)).fetchall()
     conn.close()
 
     results = [dict(row) for row in rows]
